# ============================================================
# VidHelp Analytics — Python Dependencies
# ============================================================
# Install all at once:
#   pip install -r requirements.txt
#
# Or in a virtual environment (recommended):
#   python -m venv .venv
#   source .venv/bin/activate          (Mac/Linux)
#   .venv\Scripts\activate             (Windows)
#   pip install -r requirements.txt
# ============================================================

# ── Machine learning ─────────────────────────────────────────
scikit-learn>=1.4.0         # LinearRegression + R² / MAE metrics

